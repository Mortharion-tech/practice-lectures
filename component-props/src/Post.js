/* export default function Post(props) {
    return (
        <h1>
            {props.postId}
        </h1>
    )
} */

export default function Post({ postId }) {
    return (
        <h1>
            {postId}
        </h1>
    )
}